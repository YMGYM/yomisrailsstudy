<h1>Test#show</h1>
<p>Find me in app/views/test/show.html.erb</p>

<p>
  <strong>Title</strong>
  <%= @test.title %>
</p>

<p>
  <strong>Content</strong>
  <%= @test.content %>
</p>

<%= link_to "뒤로가기", test_index_path %>
<%= link_to "수정하기", edit_test_path(@test) %>
<%= link_to "삭제하기", test_path(@test), method: :delete , data: {confirm: "ㄹㅇ?"}%>

<%= form_with url: "/comments/create/#{@test.id}", method: "post" do %>
  <%= label_tag :content, "댓글을 달아주세요" %>
  <%= text_field_tag(:content) %>
  <%= submit_tag "제출" %>
<% end %>

<p>
  <strong>댓글</strong>
</p>

<% @test.comments.each do |c| %>
  <p><%= c.id %> : <%= c.content %></p>
<% end %>

for i in range(0,10):

aa = [1,2,3,4,5]
for a in aa:

for(i = 0; i < len(aa); i++){
  aa[i]
